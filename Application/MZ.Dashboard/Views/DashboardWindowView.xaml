<mah:MetroWindow x:Class="MZ.Dashboard.Views.DashboardWindowView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                 xmlns:prism="http://prismlibrary.com/" 
                 xmlns:converters="clr-namespace:MZ.Resource.Converters;assembly=MZ.Resource"
                 xmlns:loading="clr-namespace:MZ.Loading.Views;assembly=MZ.Loading"
                 xmlns:viewmodels="clr-namespace:MZ.Dashboard.ViewModels" 
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                 d:DataContext="{d:DesignInstance Type=viewmodels:DashboardWindowViewModel}"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 mc:Ignorable="d"
                 prism:ViewModelLocator.AutoWireViewModel="True"
                 Background="{DynamicResource MahApps.Brushes.ThemeBackground}"
                 WindowState="Maximized"
                 Title="MZ.Application">
    <Window.Resources>
        <converters:BooleanInverterConverter x:Key="BooleanInverterConverter"/>
        <converters:VisibilityConverter x:Key="VisibilityConverter"/>
        <converters:LanguageConverter x:Key="LanguageConverter"/>
    </Window.Resources>

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closing">
            <i:InvokeCommandAction Command="{Binding Path=WindowClosingCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <mah:MetroWindow.LeftWindowCommands>
        <mah:WindowCommands>
            <Button ToolTip="ToolTip">
                <Image Source="pack://application:,,,/MZ.Resource;component/Assets/Images/loading.gif"
                       Width="22"
                       Height="22"/>
            </Button>
        </mah:WindowCommands>
    </mah:MetroWindow.LeftWindowCommands>
    <mah:MetroWindow.RightWindowCommands>
        <mah:WindowCommands>
            <ItemsControl ItemsSource="{Binding Path=WindowCommandButtons}" >
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel HorizontalAlignment="Stretch"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style>
                        <Setter Property="FrameworkElement.Margin" Value="0,4,0,0"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding Path=Command}" 
                                Visibility="{Binding Path=IsVisibility, Converter={StaticResource VisibilityConverter}}">
                            <Button.ToolTip>
                                <ToolTip Style="{StaticResource CustomToolTipStyle}"
                                         Content="{Binding Path=Tooltip, Converter={StaticResource LanguageConverter}}" />
                            </Button.ToolTip>
                            <iconPacks:PackIconMaterial Kind="{Binding Path=IconKind}" Width="20" Height="20"/>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </mah:WindowCommands>
    </mah:MetroWindow.RightWindowCommands>
    <Grid>
        <ContentControl prism:RegionManager.RegionName="DashboardRegion"/>
        <loading:LoadingView DataContext="{Binding Path=LoadingModel}"/>
    </Grid>
</mah:MetroWindow>
