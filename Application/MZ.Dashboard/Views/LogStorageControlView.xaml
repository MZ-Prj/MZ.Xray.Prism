<UserControl x:Class="MZ.Dashboard.Views.LogStorageControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:converters="clr-namespace:MZ.Resource.Converters;assembly=MZ.Resource"
             xmlns:prism="http://prismlibrary.com/" 
             xmlns:viewmodels="clr-namespace:MZ.Dashboard.ViewModels" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             d:DataContext="{d:DesignInstance Type=viewmodels:LogStorageControlViewModel}"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="4">
            <DatePicker Margin="0,0,4,0" FontSize="12" SelectedDate="{Binding Path=StartSelectedDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <DatePicker Margin="0,0,4,0" FontSize="12" SelectedDate="{Binding Path=EndSelectedDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <ComboBox ItemsSource="{Binding Path=LogLevels}" 
                      SelectedItem="{Binding Path=SelectedLogLevel, Mode=TwoWay}"
                      Margin="0,0,4,0" 
                      Width="120">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Path=Text}" 
                                   FontWeight="Bold"
                                   Foreground="{Binding Path=ColorBrush}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Button Margin="0,0,4,0" Command="{Binding Path=SearchCommand}">
                <iconPacks:PackIconMaterial Kind="TableSearch"  Width="16" Height="16"/>
            </Button>
            <Border CornerRadius="4"
                    BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                    BorderThickness="1"
                    Margin="0,0,4,0"
                    Width="320">
                <Grid VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <iconPacks:PackIconMaterial Margin="4" 
                                        Grid.Column="0" 
                                        Kind="Magnify"/>
                    <TextBox Grid.Column="1"
                             BorderThickness="0"
                             FontSize="12"
                             Text="{Binding Path=SearchLogText, UpdateSourceTrigger=PropertyChanged}"
                             VerticalAlignment="Center"
                             Background="Transparent"/>
                </Grid>
            </Border>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Path=Logs, Mode=TwoWay}" 
                  Grid.Row="1" 
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Hidden"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  CanUserDeleteRows="False"
                  HeadersVisibility="Column"
                  Margin="4">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" 
                                    Binding="{Binding Path=Date, StringFormat=yyyy-MM-dd}" 
                                    Width="Auto" />
                <DataGridTextColumn Header="Time" 
                                    Binding="{Binding Path=Date, StringFormat=HH:mm:ss.fff}" 
                                    Width="Auto" />
                <DataGridTextColumn Header="LogLevel" 
                                    Binding="{Binding Path=LogLevel}" 
                                    Width="Auto">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center" />
                            <Setter Property="Foreground" Value="{Binding Path=ColorBrush}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Line" 
                                    Binding="{Binding Path=LineNumber}" 
                                    Width="Auto">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold" />
                            <Setter Property="HorizontalAlignment" Value="Right" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Message" 
                                    Binding="{Binding Path=Message}" 
                                    Width="*" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
