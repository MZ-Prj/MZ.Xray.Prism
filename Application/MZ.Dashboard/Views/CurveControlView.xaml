<UserControl x:Class="MZ.Dashboard.Views.CurveControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:converters="clr-namespace:MZ.Resource.Converters;assembly=MZ.Resource" 
             xmlns:behaviors="clr-namespace:MZ.Dashboard.Bahaviors"
             xmlns:prism="http://prismlibrary.com/" 
             xmlns:viewmodels="clr-namespace:MZ.Dashboard.ViewModels" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             d:DataContext="{d:DesignInstance Type=viewmodels:CurveControlViewModel}"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
       
        <Border Grid.Row="0" Margin="4" CornerRadius="4" BorderThickness="1" BorderBrush="{DynamicResource MahApps.Brushes.Accent4}">
            <Grid>
                <Image x:Name="HistogramCurveImage" Source="{Binding Path=Media.Model.Histogram, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <Canvas x:Name="CurveCanvas"
                        Background="Transparent"
                        Width="{Binding Source={x:Reference Name=HistogramCurveImage}, Path=ActualWidth}"
                        Height="{Binding Source={x:Reference Name=HistogramCurveImage}, Path=ActualHeight}">
                    <i:Interaction.Behaviors>
                        <behaviors:HistogramCurveBehavior />
                    </i:Interaction.Behaviors>
                </Canvas>
            </Grid>
        </Border>

        <DataGrid ItemsSource="{Binding Path=CurveSpline.Points}"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Hidden"
                  CanUserAddRows="False" 
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  HorizontalContentAlignment="Center"
                  Margin="4"
                  Width="Auto"
                  HorizontalAlignment="Stretch"
                  MaxHeight="210">
            <DataGrid.Columns>
                
                <DataGridTextColumn Header="X" Width="1*" Binding="{Binding Path=X, Mode=TwoWay}"/>
                <DataGridTextColumn Header="Y" Width="1*" Binding="{Binding Path=Y, Mode=TwoWay}"/>
                
                <DataGridTemplateColumn Header="{DynamicResource LngMaterialControlRegion_Delete}" Width="1*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Command="{Binding Path=DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}">
                                <iconPacks:PackIconMaterial Kind="Minus"/>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
