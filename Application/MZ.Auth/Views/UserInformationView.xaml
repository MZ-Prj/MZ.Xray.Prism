<UserControl x:Class="MZ.Auth.Views.UserInformationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:MZ.Resource.Converters;assembly=MZ.Resource"
             xmlns:managers="clr-namespace:MZ.Resource.Managers;assembly=MZ.Resource" 
             xmlns:prism="http://prismlibrary.com/" 
             xmlns:viewmodels="clr-namespace:MZ.Auth.ViewModels" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             d:DataContext="{d:DesignInstance Type=viewmodels:UserInformationViewModel}"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="Transparent">
    <UserControl.Resources>
        <BackEase x:Key="SpringEase" Amplitude="0.5" EasingMode="EaseOut"/>
    </UserControl.Resources>

    <Grid>
        <Border CornerRadius="16" Padding="32" 
                HorizontalAlignment="Center" VerticalAlignment="Center"
                RenderTransformOrigin="0.5,0.5">
            <Border.RenderTransform>
                <TranslateTransform x:Name="ShakeTransform"/>
            </Border.RenderTransform>
            <Border.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="ShakeTransform"
                                                           Storyboard.TargetProperty="X">
                                <EasingDoubleKeyFrame KeyTime="0:0:0.0" Value="0"/>
                                <EasingDoubleKeyFrame KeyTime="0:0:0.05" Value="-5"/>
                                <EasingDoubleKeyFrame KeyTime="0:0:0.1" Value="5"/>
                                <EasingDoubleKeyFrame KeyTime="0:0:0.15" Value="-5"/>
                                <EasingDoubleKeyFrame KeyTime="0:0:0.2" Value="5"/>
                                <EasingDoubleKeyFrame KeyTime="0:0:0.25" Value="0"/>
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Border.Triggers>

            <StackPanel HorizontalAlignment="Center" Margin="16">
                <StackPanel >

                    <TextBlock x:Name="AvatarChar"
                               Text="{Binding User.Username}"
                               FontSize="64" FontWeight="Bold"
                               HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock.RenderTransform>
                            <ScaleTransform ScaleX="0" ScaleY="0"/>
                        </TextBlock.RenderTransform>
                        <TextBlock.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="AvatarChar"
                                                         Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                         From="0" To="1" Duration="0:0:0.5" EasingFunction="{StaticResource SpringEase}"/>
                                        <DoubleAnimation Storyboard.TargetName="AvatarChar"
                                                         Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                         From="0" To="1" Duration="0:0:0.5" EasingFunction="{StaticResource SpringEase}"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </TextBlock.Triggers>
                    </TextBlock>

                    <TextBlock Text="{Binding User.Role}" FontSize="14" FontWeight="SemiBold" Opacity="0.6"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>

                    <TextBlock Text="{Binding UI.UsingDate, StringFormat='{}{0:hh\\:mm\\:ss}'}" FontSize="18" Opacity="0.5"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </StackPanel>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>
