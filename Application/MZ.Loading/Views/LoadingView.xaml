<UserControl x:Class="MZ.Loading.Views.LoadingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/" 
             xmlns:converters="clr-namespace:MZ.Resource.Converters;assembly=MZ.Resource">

    <UserControl.Resources>
        <converters:VisibilityConverter x:Key="VisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch"
          Background="#80000000"
          Visibility="{Binding Path=IsLoading, Converter={StaticResource VisibilityConverter}}">
        <Border HorizontalAlignment="Center"
                VerticalAlignment="Center"
                CornerRadius="8" 
                BorderThickness="2"
                BorderBrush="{DynamicResource MahApps.Brushes.Gray8}"
                Background="{DynamicResource MahApps.Brushes.ThemeBackground}" 
                Padding="16">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="{Binding Path=Message}" 
                           FontSize="16" 
                           Margin="0,0,0,8"
                           HorizontalAlignment="Center"/>
                <ProgressBar IsIndeterminate="True" Width="240" Height="20"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
