<UserControl x:Class="MZ.Toolbar.Views.ToolbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <ToolBarTray Background="{DynamicResource MahApps.Brushes.Gray10}">
        <ToolBar Band="0" BandIndex="0">
            <ItemsControl ItemsSource="{Binding Path=Menus}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ToolBarPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding Path=Command}" ToolTip="{Binding Path=ToolTip}" >
                            <iconPacks:PackIconMaterial Kind="{Binding Path=IconKind}" Width="20" Height="20"/>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ToolBar>
    </ToolBarTray>
</UserControl>
